import SwiftUI

struct HealthDataUnavailableView: View {
    var body: some View {
        Text("Health data is unavailable.")
    }
}

struct HealthDataUnavailableView_Previews: PreviewProvider {
    static var previews: some View {
        HealthDataUnavailableView()
    }
}
